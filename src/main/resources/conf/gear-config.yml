#
# Mongodb
#
mongodb:
   sii-list:
      #mode: sync, async
      mode: sync
      url: ${sys:${env:ENVIRONMENT}.mongodb.MONGODB_URL}
      database: ${sys:${env:ENVIRONMENT}.mongodb.MONGODB_DATABASE}
      collection: ${sys:${env:ENVIRONMENT}.mongodb.MONGODB_COLLECTION}
      timeout: 5
      query: {}
      filter:
      sort: false
      limit: 0
     
